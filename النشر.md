# دليل النشر — Google Sheets + Google Apps Script Web App

هذا الدليل ينقل النظام من "يعمل محليًا بمحاكاة (Sandbox)" إلى "متصل فعليًا بـ Google Sheets حقيقي". لم تُنفَّذ هذه الخطوات من بيئة تجهيز هذه الحزمة (راجع `docs/اختبار_Live_Integration.md` للسبب)، لذا اتبعها بدقة من جهازك، ويُفضَّل على Google Sheet تجريبي منفصل عن أي بيانات إنتاج حقيقية في أول مرة.

## 1. إنشاء الشيت

1. أنشئ Google Sheet جديدًا فارغًا في حسابك.
2. File → Import → Upload → اختر `backend/ARRIVE_Backend.xlsx` → **Replace spreadsheet**.
   - هذا الملف يحتوي رؤوس أعمدة فقط لكل تاب من التابات التسعة (7 أنواع رقابة + "الإعدادات" + "`_Sequence`") — بدون أي بيانات تشغيلية حقيقية.
   - بديل: أعد توليده بنفسك من `backend/schema.json` عبر `python3 backend/build_backend.py` إن أردت التأكد بنفسك أنه يُبنى مباشرة من نفس تعريف الحقول المستخدم في الاختبارات.

## 2. لصق الكود

1. من داخل الشيت: **Extensions → Apps Script**.
2. احذف أي كود افتراضي (`function myFunction() {}` وما شابه).
3. الصق محتوى `appsscript/Code.gs` بالكامل (2000+ سطر تقريبًا — تأكد من نسخه كاملًا دون قطع).
4. احفظ (Ctrl+S / Cmd+S).

## 3. النشر كـ Web App

1. أعلى يمين محرر Apps Script: **Deploy → New deployment**.
2. اختر نوع النشر: **Web app** (⚙️ Select type إن لم يظهر تلقائيًا).
3. الإعدادات:
   - **Execute as:** Me (حسابك — هذا يعني أن الكود يعمل بصلاحياتك أنت دائمًا، بغض النظر عمّن يستخدم الواجهة).
   - **Who has access:** حدّدها حسب سياسة مؤسستكم الداخلية. إن كانت الواجهة ستُستخدم فقط من داخل نطاق حسابات Google Workspace الخاص بـ ARRIVE، اختر الخيار المناسب لذلك بدل "Anyone" لتقليل سطح التعرّض.
4. اضغط **Deploy**. ستظهر رسالة طلب أذونات (Authorization) — وافق عليها (هذا طبيعي، لأنك تُشغِّل كودًا يكتب في شيتك أنت).
5. انسخ **Web app URL** الظاهر — ينتهي بـ `/exec`. هذا هو الرابط الحقيقي المطلوب في شاشة إعدادات Dashboard.

## 4. ربط Dashboard بالرابط الحقيقي

1. افتح `dashboard/index.html`.
2. في حقل الرابط، الصق رابط `/exec` الحقيقي (وليس رابط المحرر `script.google.com/.../edit`، ولا `XXXX`، ولا أي رابط تجريبي وهمي).
3. اضغط الدخول. تفشل المحاولة برسالة واضحة إن كان الرابط خاطئًا أو النشر غير مكتمل، ولن يُحفَظ الرابط في هذه الحالة.
4. عند النجاح، جرّب حفظ سجل تجريبي واحد من أي نوع، ثم ارجع للشيت وتحقق يدويًا من ظهور الصف في التاب الصحيح بالحقول الصحيحة.

## 5. إعادة النشر بعد أي تعديل على Code.gs

كل تعديل على `Code.gs` يتطلب نشرًا جديدًا ليدخل حيز التنفيذ:
**Deploy → Manage deployments → ✏️ (تعديل) → Version: New version → Deploy**.
تحديث الكود في المحرر وحده **لا يكفي** — التعديل لا ينعكس على رابط `/exec` القائم إلا بإصدار جديد.

## ملاحظات أمان

- لا يُطلب في أي خطوة كلمة مرور أو مفتاح API أو ملف Service Account.
- لا تضع رابط `/exec` الحقيقي داخل أي ملف يُرفع إلى GitHub — يُدخَل فقط يدويًا في شاشة إعدادات Dashboard، ويُخزَّن محليًا في `localStorage` بمتصفحك فقط.
- إن كنت تختبر أولًا، استخدم Google Sheet **تجريبيًا منفصلًا تمامًا** عن أي شيت إنتاج حقيقي، حتى تتأكد من سلامة كل شيء قبل التحويل للشيت الفعلي.
