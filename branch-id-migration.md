# دليل هجرة معرّف الفرع (Branch ID Migration)

## تنبيه أولي مهم

**الموجود فعليًا في هذه النسخة هو أداة معاينة/محاكاة (Preview/Simulation) فقط.** لا توجد أي أداة
تنفّذ هجرة فعلية تكتب على أي شيت — لا في `appsscript/Code.gs` ولا في أي سكربت آخر بالمشروع. أي كلام
في هذا الملف عن "تنفيذ Migration فعلية لاحقًا" هو **خطة مستقبلية غير مبنية بعد**، وليس وصفًا لشيء
موجود. لا تعتمد على هذا الملف كدليل لتنفيذ هجرة حقيقية على بيانات إنتاجية.

---

## 1) الهدف من Branch ID

قبل هذه المرحلة، كانت السجلات التشغيلية في التابات السبعة (المطابقة الصباحية، مطابقة المرتجعات، مسح
الحالات، التحويلات، المشاكل اليومية، الجزاءات، الحالات المتأخرة) تحدد الفرع باسمه النصي فقط (عمود
"الفرع"). المشكلة: اسم الفرع نص قابل للتغيير (إعادة تسمية) وقابل للتكرار بالخطأ، فلا يصلح كمفتاح
ربط دائم وموثوق بين السجل والفرع الفعلي عبر الزمن.

**Branch ID** معرّف داخلي ثابت (مثل `BR-001`) يُنشأ مرة واحدة عند إضافة الفرع ولا يتغيّر أبدًا طوال
عمر الفرع، بصرف النظر عن أي إعادة تسمية لاحقة لاسمه المعروض.

## 2) الفرق بين Branch ID وBranch Name

| | Branch ID | Branch Name |
|---|---|---|
| الثبات | ثابت مدى الحياة | قابل للتغيير (إعادة تسمية) |
| الفريدة | فريد دومًا، يُمنع تكراره | يُمنع تكراره بين الفروع **النشطة** فقط — قد يتكرر تاريخيًا بين فرع نشط وآخر معطَّل بنفس الاسم القديم |
| الاستخدام | الربط الداخلي/التاريخي، الصلاحيات، Branch Scope | العرض للمستخدم فقط |
| مصدره | يُولَّد تلقائيًا (`_nextBranchId`) | يُدخِله المسؤول يدويًا عند الإضافة/إعادة التسمية |

## 3) التابات السبعة المتأثرة

المطابقة الصباحية (Morning)، مطابقة المرتجعات (Returns)، مسح الحالات (StatusScrub)، التحويلات
(Transfers)، المشاكل اليومية (DailyIssues)، الجزاءات (Penalties)، الحالات المتأخرة (Overdue). كل
تاب من هذه السبعة يملك عمود "الفرع" (اسم نصي، إلزامي) بالإضافة إلى عمود محسوب "معرّف الفرع
(داخلي)" يُملأ تلقائيًا عند كل عملية حفظ/تعديل جديدة (`branch_id_lookup` في `backend/schema.json`)
— لكن هذا العمود المحسوب لم يكن موجودًا قبل هذه الميزة، فالسجلات القديمة (المُدخَلة قبل تفعيلها) قد
لا تحمل قيمة فيه إطلاقًا.

## 4) طريقة عمل الربط (Mapping)

أداة المعاينة (`_migrationPreviewBranches()` في `Code.gs`) تعالج كل سجل في كل تاب من السبعة كالتالي:

1. إن كان السجل يحمل بالفعل معرّف فرع محسوبًا (من عملية حفظ حديثة)، تتحقق الأداة أن هذا المعرّف لا
   يزال يطابق فرعًا موجودًا فعليًا في قائمة الفروع الحالية.
2. إن لم يحمل السجل أي معرّف (سجل قديم)، تحاول الأداة مطابقة اسم الفرع المُخزَّن في السجل باسم فرع
   واحد **مطابق تمامًا** (Exact Match) ضمن الفروع الحالية (نشطة أو معطَّلة على حد سواء).
3. لا تُستخدم أي مطابقة تقريبية أو "أقرب اسم" — فقط تطابق كامل حرفي، تفاديًا لربط خاطئ صامت.

## 5) الحالات الخمس التي تصنّفها الأداة

| الحالة | المعنى |
|---|---|
| **مرتبط (Linked)** | معرّف محفوظ صالح، أو اسم فرع فريد يطابق فرعًا واحدًا بالضبط |
| **غير مرتبط (Unlinked)** | لا يوجد اسم فرع مسجَّل بالسجل إطلاقًا (حقل فارغ) |
| **اسم غير معروف (Unknown Name)** | اسم الفرع في السجل لا يطابق أي فرع ضمن القائمة الحالية (رُبما فرع أُعيدت تسميته دون تحديث السجلات القديمة، أو خطأ إملائي تاريخي) |
| **تكرار اسم (Duplicate Name)** | اسم الفرع في السجل يطابق أكثر من فرع واحد بنفس الاسم بالضبط — لا يمكن حسم أيهما تلقائيًا |
| **يحتاج مراجعة (Needs Review)** | معرّف فرع محفوظ سابقًا لكنه لم يعد يطابق أي فرع ضمن القائمة الحالية (يشمل أيضًا حالة "تكرار اسم") |

## 6) طريقة تشغيل Migration Preview

عبر Backend، إجراء `doPost` باسم `migrationPreviewBranches`، متاح فقط لمن يملك صلاحية
`manageBranches` (Administrator حاليًا). يُرجع تقريرًا JSON يحتوي لكل تاب: الإجمالي، عدد المرتبط،
غير المرتبط، يحتاج المراجعة، تكرار الاسم، الاسم غير المعروف، وعيّنة (حتى 20 سجلًا) من كل حالة
إشكالية مع السبب ومعرّف السجل. لا واجهة رسومية مخصصة لهذا التقرير في `dashboard/index.html` بعد —
يُستدعى حاليًا فقط برمجيًا أو عبر أداة اختبار مباشرة (`appsscript/test_migration_preview.js`).

## 7) عدم وجود كتابة فعلية أثناء المعاينة

الدالة **قراءة فقط بشكل مطلق**: لا تستدعي `setValue`/`appendRow`/`insertRowBefore` أو أي دالة كتابة
على أي شيت إطلاقًا. مُثبَت باختبار فعلي (`test_migration_preview.js`) يقارن عدد صفوف كل تاب من
السبعة وشيت "الإعدادات" قبل تشغيل الأداة وبعده — يجب أن يتطابق العددان تمامًا. التقرير نفسه يعلن
صراحة `"mode": "PREVIEW_ONLY_NO_WRITES"`.

**الاستثناء الوحيد**: تشغيل الأداة نفسه يُسجَّل كعملية في سجل العمليات (Audit Log) — وهذا سلوك
مقصود ومتَّسق مع تسجيل أي عملية إدارية أخرى، وليس كتابة على بيانات الهجرة نفسها.

## 8) كيفية مراجعة النتائج

1. شغِّل `migrationPreviewBranches` واحصل على التقرير الكامل.
2. لكل تاب، راجع أولًا نسبة "مرتبط" إلى الإجمالي — نسبة مرتفعة تعني أن أغلب البيانات قابلة للربط
   التلقائي بثقة.
3. راجع عيّنات "اسم غير معروف" يدويًا: هل هي أسماء فروع قديمة أُعيدت تسميتها لاحقًا؟ إن كان كذلك،
   القرار الإداري هو إما (أ) إبقاء السجل التاريخي باسمه القديم دون ربط، أو (ب) إنشاء جدول ربط يدوي
   اسم-قديم → Branch ID الحالي (غير موجود بعد في الكود — خطوة مستقبلية).
4. راجع عيّنات "تكرار اسم": هذه غالبًا خطأ بيانات (فرعان بنفس الاسم بالضبط) يجب حسمه إداريًا (دمج أو
   إعادة تسمية أحدهما) قبل أي ربط تلقائي.
5. لا تُعتمَد أي نتيجة ربط تلقائي نهائيًا قبل مراجعة بشرية لعيّنة كافية منها على الأقل.

## 9) كيفية إنشاء نسخة احتياطية (قبل أي هجرة فعلية مستقبلية)

- **File → Make a copy** على جدول Google Sheets بالكامل من واجهة Google Sheets نفسها، قبل أي تعديل.
- الاحتفاظ بالنسخة في مجلد Google Drive منفصل يحمل تاريخ النسخة الاحتياطية في اسمه.
- عدم الاعتماد على أي نسخة احتياطية محلية داخل هذا المستودع (`.gitignore` يستبعد صراحةً أي ملف يحمل
  "backup" أو "production" في اسمه لمنع رفعه بالخطأ إلى GitHub).

## 10) تنفيذ Migration فعلية لاحقًا (غير موجود بعد)

لا توجد بعد أداة تكتب معرّف الفرع المُستنتَج فعليًا داخل عمود السجلات القديمة. عند بناء هذه الأداة
مستقبلًا، يجب أن تلتزم — كحد أدنى — بما يلي (متطلبات، وليست ميزات موجودة):

- تعمل فقط على السجلات المصنَّفة "مرتبط" بثقة كاملة (تطابق فريد) — أبدًا على "تكرار اسم" أو "اسم
  غير معروف" تلقائيًا.
- تتطلب تأكيدًا إداريًا صريحًا منفصلًا عن تشغيل المعاينة (لا تُنفَّذ تلقائيًا فور نجاح Preview).
- تُنشئ نسخة احتياطية تلقائية (أو تتحقق من وجود واحدة حديثة) قبل أي كتابة.
- تُنتج تقرير ما-بعد-التنفيذ يقارَن بتقرير المعاينة قبله.

## 11) طريقة الاسترجاع (Rollback)

بما أن أداة المعاينة الحالية لا تكتب شيئًا، **لا حاجة لأي Rollback الآن** — لا يوجد تغيير يُراجَع.
عند بناء أداة التنفيذ الفعلية مستقبلًا، يكون الـ Rollback المتاح فوريًا هو استرجاع نسخة Google Sheets
الاحتياطية الكاملة (بند 9 أعلاه) عبر **File → Version history → Restore this version** في Google
Sheets، أو استبدال الشيت بالكامل من نسخة "Make a copy" المحفوظة قبل التنفيذ.

## 12) المخاطر المحتملة

| الخطر | الأثر | كيف تُخفَّف حاليًا |
|---|---|---|
| ربط تلقائي خاطئ لاسم متشابه لكن غير مطابق فعليًا | تلوّث بيانات تاريخية بمعرّف فرع خاطئ | الأداة تستخدم تطابقًا حرفيًا كاملًا فقط، لا تخمينًا تقريبيًا |
| تنفيذ هجرة فعلية دون نسخة احتياطية | فقدان دائم للبيانات الأصلية | لا توجد أداة تنفيذ فعلية بعد؛ حين تُبنى، نسخة احتياطية مسبقة إلزامية (بند 10) |
| الخلط بين "معاينة" و"تنفيذ فعلي" عند التواصل مع الإدارة | قرار إداري مبني على افتراض خاطئ أن البيانات أُصلحت فعليًا | كل تقرير يحمل `mode: "PREVIEW_ONLY_NO_WRITES"` صراحة، وهذا الملف نفسه يكرر التنبيه في مقدمته |
| فروع بنفس الاسم بالضبط ناتجة عن خطأ إدخال تاريخي | تعطيل الربط التلقائي لكل السجلات المرتبطة باسم مكرر | تُصنَّف "تكرار اسم" وتُعرَض صراحة للمراجعة اليدوية، لا تُربَط تلقائيًا أبدًا |
