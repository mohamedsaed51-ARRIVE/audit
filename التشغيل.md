# دليل التشغيل

## تشغيل الواجهة (Dashboard) فقط

`dashboard/index.html` ملف واحد قائم بذاته — كل الـ HTML وCSS وJavaScript مضمَّنة بداخله، ولا يحمّل أي ملف خارجي ولا أي مكتبة من الإنترنت. لذلك:

- افتحه مباشرة بالمتصفح (نقر مزدوج، أو Open With → المتصفح).
- أو، إن أردت خدمته عبر خادم ملفات ثابت محليًا (اختياري تمامًا، وليس ضروريًا):
  ```bash
  cd dashboard
  python3 -m http.server 8000
  # ثم افتح http://localhost:8000/index.html
  ```

عند أول فتح ستظهر شاشة إعداد تطلب رابط Web App الحقيقي (راجع `docs/النشر.md`) واسمك. بدون رابط صحيح ومتصل فعليًا، لن تتجاوز شاشة الإعداد — هذا سلوك مقصود، وليس عطلًا.

## تشغيل الاختبارات المحلية

**لا تحتاجها لمجرد استخدام Dashboard** — فقط إن أردت التحقق من سلامة منطق `Code.gs` بنفسك قبل النشر، أو بعد أي تعديل عليه.

```bash
npm install
npx playwright install chromium   # أول مرة فقط
npm run test:all
```

كل ملف اختبار يمكن تشغيله منفردًا أيضًا:
```bash
node tests/test_harness.js       # Node.js — لا يحتاج Playwright
node tests/test_rule_parity.js   # لا يحتاج Playwright
node tests/test_dashboard.js     # يحتاج Playwright + Chromium مثبَّتين
```

### ماذا لو فشل تثبيت Playwright أو تحميل Chromium؟

`test_harness.js` و`test_rule_parity.js` لا يعتمدان على Playwright إطلاقًا (فقط `fs`/`path`/`vm` من مكتبات Node.js القياسية) — يمكن تشغيلهما دائمًا بمجرد وجود Node.js. أما `test_dashboard.js` وحده هو من يحتاج Playwright وChromium فعليًا.

## أخطاء متوقعة وحلولها

| الخطأ | السبب المحتمل | الحل |
|---|---|---|
| شاشة الإعداد ترفض الرابط دائمًا | الرابط لا ينتهي بـ `/exec`، أو النشر (Deployment) غير مكتمل، أو "Who has access" لا يسمح لحسابك | راجع `docs/النشر.md` خطوة بخطوة |
| `Cannot find module 'playwright'` عند تشغيل `test_dashboard.js` | لم يُنفَّذ `npm install` | نفّذه ثم أعد المحاولة |
| Chromium لا يُفتح / خطأ "Executable doesn't exist" | لم يُنفَّذ `npx playwright install chromium` | نفّذه — يحمّل نسخة Chromium الخاصة بـ Playwright تلقائيًا |
| بيانات تظهر أصفارًا في لوحة KPIs | إما لا توجد سجلات فعلية بعد في الشيت، أو فشل الاتصال بالخادم — الواجهة تُفرّق بين الحالتين برسالة نصية واضحة أسفل المؤشرات، فراجعها قبل افتراض عطل |
