{
  "name": "arrive-audit-control-tower",
  "version": "1.0.0",
  "private": true,
  "description": "ARRIVE AUDIT CONTROL TOWER — Dashboard (Google Apps Script + Google Sheets backend)",
  "scripts": {
    "test:node": "node tests/test_harness.js",
    "test:parity": "node tests/test_rule_parity.js",
    "test:browser": "node tests/test_dashboard.js",
    "test:all": "npm run test:node && npm run test:parity && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
