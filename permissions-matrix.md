# مصفوفة الصلاحيات — ARRIVE AUDIT CONTROL TOWER (V3)

هذا الملف هو **المرجع الوحيد الموثوق** للصلاحيات الفعلية في النظام. كل سطر هنا مأخوذ حرفيًا من
`backend/schema.json` (المفتاح `role_permissions`) والمُطبَّق فعليًا داخل `appsscript/Code.gs` —
وليس وصفًا نظريًا منفصلًا عن الكود. عند أي تعارض بين هذا الملف والكود، الكود هو المرجع، ويجب تحديث
هذا الملف فورًا (راجع قسم "آلية ضمان التطابق" في الأسفل).

**النظام مخصص حصريًا لموظفي قسم التدقيق الداخلي في ARRIVE.** لا يوجد أي دور لموظفي الفروع، أو
Operations، أو Customer Service، أو أي مستخدم خارجي. تم إلغاء دور "Branch Manager" نهائيًا من هذه
النسخة (أُعيدت تسميته إلى **Senior Auditor** بنفس الصلاحيات بالضبط — راجع CHANGELOG.md).

---

## الأدوار الخمسة المعتمدة

| # | الدور | الوصف المختصر |
|---|---|---|
| 1 | **Administrator** | مسؤول النظام — إدارة كاملة: الفروع، الموظفين، الإعدادات، بالإضافة لكل صلاحيات التدقيق |
| 2 | **Audit Manager** | مدير تدقيق — يشرف على عمليات التدقيق ويملك صلاحية القراءة/الكتابة الكاملة ومراجعة سجل العمليات ضمن الفروع المكلَّف بها (أو كل الفروع عند تفويض صريح) |
| 3 | **Senior Auditor** | مدقق أول — نفس صلاحيات Auditor + عرض التقارير وسجل العمليات، ضمن فروعه المكلَّف بها فقط |
| 4 | **Auditor** | مدقق — إدخال وتعديل السجلات التشغيلية ضمن فروعه المكلَّف بها فقط |
| 5 | **Audit Viewer** | عرض فقط — بلا أي صلاحية حفظ أو تعديل، ضمن فروعه المكلَّف بها فقط |

---

## الجدول الكامل

| الدور | عمليات القراءة (Read) | عمليات الإضافة (Create) | عمليات التعديل (Update) | عمليات التعطيل (Disable) | إدارة الفروع | إدارة الموظفين | Branch Scope | إمكانية ALL Branches |
|---|---|---|---|---|---|---|---|---|
| **Administrator** | search, kpis, getRecord, reports, auditLog (بلا تصفية) | save (سجلات)، addBranch، addEmployee | update (سجلات)، renameBranch، updateEmployee | toggleBranch، toggleEmployee | ✅ كاملة | ✅ كاملة | `all` (ثابت على مستوى الدور — لا يتأثر بأي قيمة تُخزَّن في حقل فروعه) | ✅ **مسموحة دومًا للدور نفسه** — Administrator هو الدور الوحيد الذي يملك `manageEmployees`، وبالتالي هو المنفِّذ الوحيد القادر على منح ALL لأي دور آخر مصرَّح له بها |
| **Audit Manager** | search, kpis, getRecord, reports (كلها ضمن نطاقه)، auditLog (ضمن نطاقه ما لم يُمنَح ALL) | save (سجلات) فقط — **لا** addBranch/addEmployee | update (سجلات) فقط | لا يملك | ❌ | ❌ | `own` — **لم يعد `all` ثابتًا** (تغيير فعلي في V3، راجع CHANGELOG). يُقيَّد بفروعه المخصَّصة تمامًا مثل Senior Auditor/Auditor | ✅ ممكن، لكن **فقط بتفويض صريح من Administrator عند إنشاء/تعديل حساب هذا الموظف بعينه** (`branches: "ALL"` صراحة) — ليس افتراضيًا لكل حساب من هذا الدور |
| **Senior Auditor** | search, kpis, getRecord, reports، auditLog — كلها ضمن فروعه فقط | save (سجلات) ضمن فروعه فقط | update (سجلات) ضمن فروعه فقط، مع التحقق من الفرع القديم والجديد معًا | لا يملك | ❌ | ❌ | `own` — دائمًا | ❌ **ممنوعة كليًا** — لا يمكن منحه ALL مهما فعل Administrator |
| **Auditor** | search, kpis فقط — **لا** يملك viewReports ولا viewAuditLog | save (سجلات) ضمن فروعه فقط | update (سجلات) ضمن فروعه فقط | لا يملك | ❌ | ❌ | `own` — دائمًا | ❌ **ممنوعة كليًا** |
| **Audit Viewer** | search, kpis, reports — ضمن فروعه فقط. **لا** يملك viewAuditLog | ❌ لا يملك أي صلاحية save | ❌ لا يملك أي صلاحية update | لا يملك | ❌ | ❌ | `own` — دائمًا | ❌ **ممنوعة كليًا (قرار إداري صريح في هذه الجولة — راجع القسم أدناه)** |

**لا توجد عملية "حذف" (Delete) لأي سجل تشغيلي في النظام لأي دور إطلاقًا** — لا في الـ Backend ولا
في الواجهة. هذا ليس نقصًا؛ هو قرار تصميم قائم لضمان عدم فقدان بيانات تدقيق تاريخية. "التعطيل"
(Disable) هو أقصى ما يمكن فعله لفرع أو حساب موظف، وهو محفوظ دون حذف السجل نفسه.

---

## الصفحات (Pages) المتاحة لكل دور

| الدور | dashboard | search | reports | settings |
|---|---|---|---|---|
| Administrator | ✅ | ✅ | ✅ | ✅ |
| Audit Manager | ✅ | ✅ | ✅ | ✅ (الأقسام المتاحة فعليًا داخل settings محكومة بصلاحياته الفردية — لا يرى أقسام إدارة الفروع/الموظفين لعدم امتلاكه manageBranches/manageEmployees، لكنه يرى سجل العمليات لامتلاكه viewAuditLog) |
| Senior Auditor | ✅ | ✅ | ✅ | ❌ |
| Auditor | ✅ | ✅ | ❌ | ❌ |
| Audit Viewer | ✅ | ✅ | ✅ | ❌ |

---

## قرار إداري صريح هذه الجولة: إيقاف ALL Branches عن Audit Viewer

عند مراجعة النظام قبل التوثيق النهائي، طُرح السؤال: هل يحتاج دور "عرض فقط" (بلا أي صلاحية تعديل أو
حفظ) فعليًا لرؤية **كل الفروع معًا**؟ لم يظهر أي سيناريو تشغيلي فعلي مذكور عبر كل جولات هذا المشروع
يبرر ذلك — الحاجتان الوحيدتان الفعليتان لرؤية شاملة لكل الفروع مُغطَّاتان بالفعل بدورين آخرين:

- **Administrator**: يحتاجها لأغراض إدارة النظام نفسه.
- **Audit Manager**: يحتاجها للإشراف الرقابي، وفقط عند تفويض إداري صريح لموظف بعينه (ليست افتراضية
  حتى له).

**القرار المُنفَّذ فعليًا في الكود**: `canBeAssignedAllBranches` لدور Audit Viewer = `false` في
`backend/schema.json` و`appsscript/Code.gs` معًا. النتيجة: لا توجد أي طريقة — لا من الواجهة ولا
بطلب Backend مباشر — لإنشاء أو تعديل حساب Audit Viewer بحيث يرى كل الفروع؛ يُرفض الطلب فورًا مهما
كان منفِّذه (حتى Administrator). مُثبَت باختبار: `test_security_v2.js` — "[صلاحيات] إضافة Audit
Viewer بـ branches:\"ALL\" تُرفض الآن — الدور لم يعد يملك canBeAssignedAllBranches".

إن ظهر مستقبلًا سيناريو فعلي حقيقي يبرر خلاف ذلك (مثلًا: جهة خارجية تحتاج رؤية شاملة للعرض فقط)،
فهذا قرار إداري منفصل يجب اتخاذه صراحةً وقتها، وليس افتراضًا يُعاد تفعيله ضمنيًا.

---

## إجابات مباشرة على أسئلة "ALL Branches" الستة

| # | السؤال | الإجابة الفعلية (مُتحقَّق منها بالكود والاختبارات) |
|---|---|---|
| 1 | هل Audit Viewer يحتاج فعلًا canBeAssignedAllBranches:true؟ | **لا** — أُوقفت (راجع أعلاه) |
| 2 | هل Administrator فقط يستطيع منح ALL Branches؟ | **نعم فعليًا** — هو الدور الوحيد الذي يملك `manageEmployees`، وبالتالي المنفِّذ الوحيد القادر على استدعاء `addEmployee`/`updateEmployee` أصلًا |
| 3 | هل Audit Manager يستطيع منح ALL Branches؟ | **لا** — Audit Manager لا يملك `manageEmployees` إطلاقًا، فلا يستطيع إنشاء أو تعديل أي حساب موظف، بما فيها حسابه الخاص لغيره |
| 4 | هل يحتاج Audit Viewer إلى ALL Branches في أي سيناريو فعلي؟ | لم يُذكر أي سيناريو كهذا عبر كامل هذا المشروع — ومن ثم أُوقفت الإمكانية |
| 5 | هل يمكن تعيين ALL Branches بدون صلاحية مناسبة؟ | **لا** — `_resolveBranchAssignment()` في Code.gs يرفض `branches:"ALL"` لأي دور لا يملك `canBeAssignedAllBranches:true` في المصفوفة، بصرف النظر عمّن يرسل الطلب |
| 6 | هل القائمة الفارغة تظل مرفوضة ولا تتحول تلقائيًا إلى ALL؟ | **نعم** — `undefined`/`null`/`[]` تُرفض جميعًا بخطأ صريح؛ القيمة الوحيدة المقبولة لمنح كل الفروع هي النص الصريح `"ALL"`، ولن تُفسَّر أي قيمة أخرى على أنها ذلك |

---

## إصلاح إضافي هذه الجولة: الخلية الفارغة في مسار القراءة لم تعد تعني ALL

اكتُشف أثناء إعداد تقرير المخاطر أن دالة قراءة بيانات الموظف من الشيت (`_employeeFromRow`) كانت
تُفسِّر خلية "الفروع" الفارغة (`""`) على أنها `"ALL"` تلقائيًا — وهو تناقض مباشر مع كل ما سبق في هذا
الملف. لا يوجد مسار برمجي طبيعي (لا Bootstrap Admin ولا إضافة/تعديل موظف عبر الواجهة) يُنتج خلية
فارغة فعليًا، لكن تعديلًا يدويًا مباشرًا في Google Sheets (ممكن دومًا) كان سيؤدي لمنح ذلك الموظف كل
الفروع دون أي قرار إداري. **أُصلح**: القاعدة الآن — الفراغ/`null`/`undefined`/فواصل بلا معرّفات فعلية
كلها تعني **"لا فروع مخصَّصة" (رفض وصول)، وليس ALL أبدًا**. الاستثناء الوحيد الباقي: حساب Administrator
الأول (Bootstrap) الذي يُكتَب بـ`"ALL"` حرفيًا صراحة عند إنشائه، وهذا لا علاقة له بهذا الإصلاح. مُثبَت
بـ29 اختبارًا جديدًا (`appsscript/test_branch_empty_no_all.js`) — راجع `docs/security-risk-report.md`
(البند 3) للتفاصيل الكاملة.

## آلية ضمان التطابق بين هذا الملف والكود

هذا الملف **لا** يُنشأ يدويًا من الصفر عند كل تعديل — أي تغيير في الصلاحيات يجب أن يبدأ من
`backend/schema.json` (`role_permissions`)، ثم يُعاد تضمينه (embed) داخل `const SCHEMA` أعلى
`appsscript/Code.gs`، ثم تُحدَّث الاختبارات (`appsscript/test_security_v2.js` و
`appsscript/test_branch_scope_v3.js` هما المرجعان الرئيسيان لسلوك الصلاحيات)، ثم يُحدَّث هذا الملف
والواجهة (`dashboard/index.html`, جدول "الأدوار والصلاحيات" في تاب الإعدادات) في نفس الدفعة. لا
تُكتب في هذا الملف أي صلاحية غير موجودة فعليًا في `role_permissions` بالكود.
