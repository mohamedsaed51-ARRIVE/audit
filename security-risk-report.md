# تقرير المخاطر الأمنية — ARRIVE AUDIT CONTROL TOWER (V3)

هذا التقرير يصف **الحالة الفعلية المُتحقَّق منها بالكود والاختبارات حتى تاريخ إعداده**، وليس تقييمًا
عامًا أو تصريحًا بأن النظام "آمن بالكامل". **لا يوجد أي جزء من هذا المشروع تم اختباره على Google Apps
Script حقيقي منشور أو على Google Sheets حقيقية — كل الاختبارات المذكورة هنا Sandbox (محاكاة Node.js
تُشغِّل كود `appsscript/Code.gs` الفعلي عبر `vm`) أو Playwright (متصفح حقيقي، لكن ضد نفس المحاكاة، لا
ضد Web App منشور فعليًا).** هذا يعني أن نجاح 321/321 اختبارًا يثبت **اتساق منطق الكود مع نفسه**، وليس
سلوكه الفعلي على منصة Google الحقيقية (حدود التنفيذ، سياسات النشر، صلاحيات الوصول الفعلية على
Sheets/Apps Script) — راجع البند 12 أدناه.

---

## 1) المصادقة (Authentication)

- **الحالة**: مفعَّلة بمنطق تسجيل دخول (اسم مستخدم/كلمة مرور) عبر `_actionLogin`، لكن **معطَّلة
  افتراضيًا** (`auth_enabled: 0` في `backend/schema.json`) — يجب على المسؤول تفعيلها يدويًا (`تفعيل
  auth_enabled=1` من شاشة الإعدادات) قبل أي استخدام إنتاجي.
- **القفل المؤقت بعد محاولات فاشلة**: 5 محاولات (`LOCKOUT_MAX_ATTEMPTS`) ثم قفل 15 دقيقة
  (`LOCKOUT_MINUTES`) عبر `CacheService` ذاتي الانتهاء (وليس مقارنة تواريخ نصية — خطأ سابق تم إصلاحه).
- **الخطورة**: متوسطة. **الأثر**: لو نُشر النظام دون تفعيل `auth_enabled` عمدًا، يعمل بلا أي حماية
  دخول إطلاقًا (سلوك ما قبل ميزة المصادقة بالكامل).
- **الإجراء المطلوب**: تأكيد تفعيل `auth_enabled=1` كخطوة إلزامية في قائمة ما-قبل-الإنتاج (راجع
  README.md).
- **هل يمنع الإنتاج؟** لا يمنعه تقنيًا، لكن **نشر النظام بدون تفعيلها يُبطل كل الحمايات الأخرى في هذا
  التقرير** — يُعتبَر شرطًا فعليًا لبدء الاستخدام الحقيقي.

## 2) التفويض (Authorization / RBAC)

- **الحالة**: كل عملية كتابة وكل عملية قراءة تُعيد سجلات تشغيلية فعلية (`kpis`/`search`/`getRecord`/
  `reports`/`viewAuditLog`/`manageBranches`/`manageEmployees`/`manageSettings`) تمر عبر `_requireAuth`
  أو تحقق مكافئ — لا يوجد إجراء واحد "معفى" من الفحص باستثناء `bootstrap`/`settings` (بيانات وصفية غير
  حسّاسة، عمدًا، لرسم شاشة الدخول قبل أي مصادقة).
- **الأدوار الخمسة** (Administrator، Audit Manager، Senior Auditor، Auditor، Audit Viewer) مصدرها
  الوحيد `SCHEMA.role_permissions` (مُضمَّن من `backend/schema.json`) — لا يوجد تحقق صلاحيات مكتوب حرفيًا
  باسم دور في أي مكان آخر بالكود (راجع `docs/permissions-matrix.md`).
- **إصلاح فعلي هذه الجولة**: `_actionListBranches` كان بلا أي فحص `_requireAuth` إطلاقًا (يكشف أسماء
  ومعرّفات كل الفروع — بما فيها المعطَّلة — لأي طالب بلا تسجيل دخول). **أُصلح**: يتطلب الآن
  `manageBranches`.
- **الخطورة (قبل الإصلاح)**: متوسطة (كشف بيانات وصفية داخلية، ليس بيانات تشغيلية حسّاسة). **بعد
  الإصلاح**: مُغلَق ومُختبَر.
- **هل يمنع الإنتاج؟** لا — تم إصلاحه واختباره.

## 3) نطاق الفروع (Branch Scope)

- **القاعدة الحاكمة**: أي دور نطاقه `"own"` (Senior Auditor، Auditor، Audit Viewer، وAudit Manager
  الآن أيضًا ما لم يُمنَح ALL صراحة) مقيَّد ببيانات فروعه المخصَّصة فقط في: القراءة (`kpis`/`search`/
  `getRecord`/`reports`)، الكتابة (`save`/`update`)، وسجل العمليات (`viewAuditLog`). النقطة الوحيدة
  لفرض هذا هي `_rowMatchesFilters`/`_checkBranchScope`/الفلترة داخل `_actionAuditLog` — جميعها تُشتق
  الفحص من بيانات الجلسة الفعلية (`session.branches`)، وليس مما تدَّعيه معطيات الطلب (Parameters).
- **إصلاح فعلي هذه الجولة (خلل خطير)**: `_employeeFromRow` (مسار قراءة بيانات الموظف من الشيت) كانت
  تُفسِّر خلية "الفروع" الفارغة (`""`) على أنها `"ALL"` تلقائيًا — تناقض مباشر مع قاعدة "لا افتراض
  ضمني لـALL" المطبَّقة في مسار الحفظ. **الأثر النظري لو بقي**: تعديل يدوي مباشر لخلية في Google
  Sheets (ممكن دومًا لأن الشيت مصدر الحقيقة)، أو أي صف تالف/قديم غير مكتمل، كان سيمنح ذلك الموظف
  رؤية/كتابة **كل الفروع** دون أي قرار إداري صريح. **أُصلح**: دالة `_parseStoredBranches` الجديدة —
  الفراغ/`null`/`undefined`/فواصل بلا معرّفات فعلية ← `[]` (لا فروع = لا وصول)، وليس `"ALL"` أبدًا. مثبَت
  بـ29 اختبارًا جديدًا (`test_branch_empty_no_all.js`).
- **الخطورة (قبل الإصلاح)**: **عالية** — توسيع صلاحيات غير مقصود وصامت. **بعد الإصلاح**: مُغلَق
  ومُختبَر (Sandbox فقط — راجع البند 12).
- **قيد لم يُعالَج بعد (خارج نطاق الكود)**: لا شيء يمنع مسؤول جدول Google Sheets نفسه (خارج تطبيق
  ARRIVE Audit Control Tower تمامًا) من تعديل أي خلية يدويًا في أي وقت — هذا قيد بنيوي على منصة Google
  Sheets، وليس عطلًا في هذا الكود. **التخفيف الوحيد المتاح حاليًا**: تقييد من له صلاحية تحرير جدول
  Google Sheets نفسه (خارج تطبيق التدقيق) إلى أقل عدد ممكن من الأشخاص الموثوقين — إجراء تنظيمي، ليس
  برمجيًا.
- **هل يمنع الإنتاج؟** لا — تم إصلاح الخلل البرمجي واختباره؛ القيد البنيوي أعلاه يبقى خطرًا تشغيليًا
  دائمًا موثَّقًا (يخص أي نظام مبني على Google Sheets كمصدر حقيقة، وليس خاصًا بهذا التطبيق).

## 4) ALL Branches (الوصول الشامل لكل الفروع)

- **القاعدة**: القيمة الوحيدة المقبولة لمنح كل الفروع هي النص الصريح `"ALL"` — لا مصفوفة فارغة، لا
  `null`/`undefined`، ولا أي قيمة أخرى تُفسَّر كذلك (راجع `_resolveBranchAssignment` لمسار الحفظ،
  و`_parseStoredBranches` لمسار القراءة بعد إصلاح هذه الجولة).
- **من يملك `canBeAssignedAllBranches:true`**: Administrator (دائمًا، لأغراض إدارة النظام) وAudit
  Manager (فقط بتفويض صريح لكل حساب على حدة، وليس افتراضيًا). **Senior Auditor وAuditor وAudit Viewer
  ممنوعون كليًا** — لا يمكن منحهم ALL مهما فعل من يملك `manageEmployees`.
- **من يستطيع المنح فعليًا**: Administrator فقط عمليًا، لأنه الدور الوحيد الذي يملك `manageEmployees`
  (شرط لتنفيذ `addEmployee`/`updateEmployee` أصلًا).
- **إصلاح فعلي سابق هذه الجولة**: كانت الواجهة تُخفي منتقي الفروع بالكامل لأي دور
  `canBeAssignedAllBranches:true`، وترسل `branches: undefined` دائمًا — أي منح ALL تلقائي بحكم الدور
  لا بقرار صريح. أُصلح بإظهار المنتقي دومًا + مربع اختيار ALL منفصل + تأكيد إضافي قبل الحفظ.
- **الخطورة (قبل الإصلاحين أعلاه)**: عالية. **بعد الإصلاح**: مُغلَق ومُختبَر (راجع البندين 2 و3 و12
  في `docs/permissions-matrix.md`).
- **هل يمنع الإنتاج؟** لا — تم إصلاحه واختباره.

## 5) رمز الجلسة (Session Token)

- **الآلية**: `Utilities.getUuid()` عشوائي، مُخزَّن في `CacheService.getScriptCache()` تحت مفتاح
  `sess_<token>` مع بيانات الجلسة (لا كلمة مرور ولا Hash ولا Salt إطلاقًا — مُثبَت باختبار مخصَّص).
- **إصلاح فعلي هذه الجولة (عطل حقيقي كان سيكسر الدخول على المنصة الحقيقية)**:
  `SESSION_TTL_SECONDS` كانت مضبوطة على `8 * 60 * 60` (28800 ثانية) — **تتجاوز الحد الأقصى الحقيقي
  الموثَّق لِـ `CacheService.put()` في Google Apps Script، وهو 21600 ثانية (6 ساعات) بالضبط**. لم
  يكتشف هذا أي من الاختبارات الـ272 السابقة لأن محاكي الـ Sandbox (`gs_sandbox.js`) لم يكن يفرض هذا
  القيد المنصوصي إطلاقًا. **أُصلح**: القيمة الآن `21600`، والمحاكي نفسه عُدِّل ليفرض هذا الحد فعليًا
  (يرمي خطأً عند تجاوزه، تمامًا كما تفعل منصة Google Apps Script الحقيقية) — حارس دائم ضد تكرار هذا
  النوع من العطل مستقبلًا.
- **⚠️ لم يُتحقَّق بعد على منصة Google Apps Script حقيقية** أن استدعاء `CacheService.put()` بمدة
  21600 ثانية بالضبط ينجح فعليًا (المحاكي الآن يطابق التوثيق الرسمي المعروف لهذا الحد، لكن هذا ليس
  بديلًا عن تشغيل فعلي حقيقي — راجع البند 12).
- **مدة الجلسة الفعلية بعد الإصلاح**: 6 ساعات كحد أقصى نظري، **بلا أي ضمان** أنها ستبقى متاحة كامل
  المدة — إعادة نشر السكربت، أو تفريغ الكاش لأي سبب من طرف منصة Google، يُفقِد الجلسة قبل ذلك. أي فشل
  في قراءة الجلسة (`_sessionGet` يعيد `null`) يُعامَل كجلسة غير صالحة ويتطلب تسجيل دخول جديد — لا يوجد
  مسار "Fail-Open" (مُثبَت بـ4 اختبارات صريحة في `test_session_ttl.js`، منها اختبار "لا Fail-Open"
  المباشر عبر 3 عمليات محمية مختلفة).
- **لا يوجد أي مسار يقرأ مدة الجلسة من الطلب (`body`)** — القيمة ثابتة في الكود فقط، لا يمكن لمستخدم من
  الواجهة تمديد جلسته أو تعديل مدة الـ Token (مُتحقَّق منه بفحص مصدري مباشر في `test_session_ttl.js`).
- **Logout**: يُبطل الجلسة فورًا من الكاش (`_sessionDestroy`) — لا ينتظر انتهاء الصلاحية الطبيعية،
  مُثبَت باختبار.
- **إبطال فوري عند التعطيل**: لو عطَّل مسؤول حساب موظف بينما جلسته سارية، يُرفض أول طلب تالٍ له فورًا
  (`_requireAuth` يقرأ حالة الحساب *الحيّة* من الشيت في كل طلب، لا يعتمد على القيم المخزَّنة وقت
  الدخول) — لا ينتظر انتهاء الـ6 ساعات.
- **الخطورة (قبل إصلاح TTL)**: عالية — عطل كان سيمنع تسجيل الدخول بالكامل على منصة حقيقية (خطأ وقت
  التشغيل من `CacheService.put()`)، لا مجرد ثغرة أمنية. **بعد الإصلاح**: مُغلَق ومُختبَر في Sandbox.
- **هل يمنع الإنتاج؟** لا يمنعه بعد الإصلاح، لكن **التحقق الفعلي على Google Apps Script حقيقي يبقى
  مطلوبًا قبل أي اعتماد نهائي** (راجع البند 12).

## 6) تجزئة كلمات المرور (Password Hashing)

- **الآلية**: SHA-256 + Salt عشوائي فريد لكل حساب (`Utilities.getUuid()` كـSalt)، وليس bcrypt/Argon2/
  scrypt (خوارزميات مصمَّمة خصيصًا لتخزين كلمات المرور بمقاومة أعلى لهجمات القوة الغاشمة).
- **لا كلمة مرور واضحة تُخزَّن في أي مكان** — مُثبَت باختبار (`adminRow.indexOf("Passw0rd!") === -1`)
  والـHash مختلف تمامًا عن القيمة الأصلية.
- **الخطورة**: متوسطة. SHA-256+Salt يحمي من قراءة كلمات المرور مباشرة من الشيت، لكنه أسرع حسابيًا من
  bcrypt/Argon2 لو تسرَّب ملف/شيت الـHashes فعليًا (قابل لهجوم قوة غاشمة أسرع نسبيًا لو حدث تسرّب).
- **مناسب لـ**: أداة داخلية بعدد موظفين محدود (قسم تدقيق واحد)، وليس بمستوى أمان بنكي/عام.
- **هل يمنع الإنتاج؟** لا — مقبول للسياق الحالي (عدد مستخدمين محدود، وصول داخلي فقط)، لكن مذكور صراحة
  كقيد وليس كميزة أمان قصوى (موثَّق أيضًا في الواجهة نفسها للمسؤول).

## 7) XSS (حقن نصوص/HTML تنفيذية)

- **الآلية**: كل نص مُدخَل من المستخدم (أسماء موظفين، أسماء فروع، ملاحظات، إلخ) يمر عبر `escapeHtml()`
  قبل إدراجه في DOM — لا استخدام لـ`innerHTML` مباشرة على نص خام غير مُهرَّب في أي مكان بالواجهة.
- **مُثبَت باختبار متصفح حقيقي (Playwright)**: حقن `<img onerror=...>`/`<svg onload=...>` في اسم
  موظف/فرع لا يُنفَّذ، ويظهر كنص حرفي في الجدول — لا عنصر `<img>`/`<svg>` فعلي واحد يُنشَأ في DOM
  (`dashboard/test_xss.js`, 6/6).
- **الخطورة**: منخفضة (بعد الإصلاح والاختبار). **هل يمنع الإنتاج؟** لا.

## 8) سجل العمليات (Audit Log)

- **الحماية**: `viewAuditLog` صلاحية منفصلة (Administrator/Audit Manager/Senior Auditor فقط) — Auditor
  وAudit Viewer لا يرونه إطلاقًا.
- **نطاق العرض**: أدوار `"own"` ترى فقط عمليات فروعها (بنفس آلية Branch Scope أعلاه).
- **إصلاح فعلي هذه الجولة**: كانت إدخالات تعديل الموظف نصًّا حرًّا عامًّا ("عُدِّل: EMP-000x") بلا أي
  تفاصيل. أُصلح بتسجيل JSON منظَّم يتضمن القيمة القديمة والجديدة لكل من `role`/`branchScope`/`fullName`
  (باستخدام Branch ID لا اسم الفرع، ليبقى قابلاً للتفسير حتى لو أُعيدت تسمية الفرع لاحقًا)، **مع عدم
  تضمين كلمة المرور إطلاقًا** (يُسجَّل فقط `"password": "changed"` كعلامة، لا القيمة).
- **لا يُسجَّل نجاح كاذب**: التسجيل يحدث فقط بعد نجاح كل التحقق (لا كتابة جزئية — عطل تم إصلاحه هذه
  الجولة أيضًا في `_actionUpdateEmployee`؛ محاولة تعديل فاشلة لا تُضيف أي قيد جديد، مُثبَت باختبار).
  تشغيل أداة Migration Preview نفسه يُسجَّل (استثناء موثَّق، ليس كتابة على بيانات الهجرة).
- **الخطورة**: منخفضة. **هل يمنع الإنتاج؟** لا.

## 9) عمليات القراءة (Read Operations Protection)

- كل إجراء يعيد سجلات تشغيلية فعلية (`kpis`/`search`/`getRecord`/`reports`) يتطلب Token صالحًا وصلاحية
  محدَّدة فور تفعيل `auth_enabled=1` — لا استثناء. `listBranches`/`listEmployees` كلاهما محميان
  بـ`manageBranches`/`manageEmployees` على التوالي.
- **هل يمنع الإنتاج؟** لا.

## 10) عمليات الكتابة (Write Operations Protection)

- `save`/`update` (السجلات التشغيلية)، `addBranch`/`renameBranch`/`toggleBranch`،
  `addEmployee`/`updateEmployee`/`toggleEmployee` — جميعها خلف `_requireAuth` بالصلاحية المناسبة، مع
  فرض Branch Scope الفعلي (لا الاعتماد على الواجهة فقط) عبر `_checkBranchScope`.
- **لا حذف (Delete) لأي سجل في النظام لأي دور** — قرار تصميم مقصود لحفظ تاريخ التدقيق (تعطيل فقط).
- **منع تصعيد الصلاحيات الذاتي**: لا يمكن لأي مستخدم (حتى Administrator) تعديل دوره أو نطاق فروعه
  الخاص من حسابه نفسه — مُثبَت باختبار.
- **هل يمنع الإنتاج؟** لا.

## 11) التخزين المحلي في المتصفح (Local Storage)

- **ما يُخزَّن فعليًا**: `arrive_api_url` (رابط الـ Backend) و`arrive_user` (الاسم الكامل المعروض
  فقط) — لا شيء آخر.
- **ما لا يُخزَّن إطلاقًا**: الـToken (متغيّر JS في الذاكرة فقط — يُفقَد عند تحديث الصفحة، يتطلب دخولًا
  جديدًا)، كلمة المرور، الـHash/الـSalt.
- **الخطورة**: منخفضة. **هل يمنع الإنتاج؟** لا.

## 12) الأسرار والبيانات الحساسة / التحقق الحقيقي غير المُنفَّذ

- **لا سر واحد (API Key/Password حرفية/رابط Web App حقيقي) موجود داخل أي ملف بالمستودع** — تم فحص
  الكود يدويًا هذه الجولة (راجع أيضًا جدول فحص GitHub المنفصل عند تجهيز الحزمة النهائية).
- **⚠️ الأهم: لم يُشغَّل أي جزء من هذا النظام على Google Apps Script حقيقي منشور، ولا على Google
  Sheets حقيقية، عبر كامل هذا المشروع حتى تاريخه.** كل رقم "نجاح" في أي تقرير اختبار سابق أو حالي —
  بما فيها 321/321 هذه الجولة — يخص محاكاة Sandbox (Node.js عبر `vm`) أو Playwright ضد نفس المحاكاة.
  هذا يثبت **اتساق منطق الكود الداخلي**، لكنه **لا يثبت**:
  - أن `CacheService.put()` يقبل فعليًا 21600 ثانية بالضبط على المنصة الحقيقية (راجع البند 5).
  - أن أذونات نشر الـWeb App (Execute as / Who has access) مضبوطة بشكل صحيح.
  - أن صلاحيات الوصول الفعلية على جدول Google Sheets نفسه (من يستطيع فتحه/تحريره خارج التطبيق) مقيَّدة
    بما يتوافق مع افتراضات هذا النظام.
  - أداء النظام أو حدود التنفيذ (Quotas) الفعلية لـApps Script تحت استخدام حقيقي.
- **الإجراء المطلوب قبل أي اعتماد إنتاجي فعلي**: نشر تجريبي حقيقي + تشغيل سيناريوهات الدخول/الجلسة/
  الصلاحيات الأساسية يدويًا على ذلك النشر، مع مراجعة إعدادات مشاركة جدول Google Sheets نفسه.
- **هل يمنع الإنتاج؟** **نعم — هذا هو الفجوة الوحيدة التي تمنع اعتبار النظام "جاهزًا للإنتاج" فعليًا**،
  بصرف النظر عن عدد اختبارات Sandbox/Playwright الناجحة.

## 13) نطاق النظام (Scope Enforcement)

- النظام مخصَّص حصريًا لموظفي قسم التدقيق الداخلي — لا يوجد أي دور لموظفي الفروع/Operations/Customer
  Service. دور "Branch Manager" السابق أُعيدت تسميته إلى "Senior Auditor" (نفس الصلاحيات بالضبط، بلا
  أي علاقة بموظفي الفروع الفعليين).
- **هل يمنع الإنتاج؟** لا — تم التحقق والتوثيق.

## 14) ملخص: هل النظام "آمن بالكامل"؟

**لا** — ولا يدَّعي هذا التقرير ذلك. الحالة الدقيقة: منطق الصلاحيات والـBranch Scope وSession وXSS
**متّسق داخليًا ومختبَر بشكل شامل في Sandbox/Playwright (321/321)**، مع إصلاح 3 أعطال حقيقية خطيرة هذه
الجولة (فحص `listBranches`، الخلية الفارغة=ALL، وSESSION_TTL_SECONDS يتجاوز حد المنصة). لكن **لا يوجد
أي إثبات تشغيلي على Google Apps Script/Google Sheets حقيقيين** — هذا هو الشرط الوحيد المتبقي فعليًا
قبل اعتبار النظام جاهزًا للإنتاج الحقيقي (البند 12).
