# تقرير الاختبارات — هذه الحزمة تحديدًا

**تشغيل فعلي حديث نُفِّذ عند تجهيز هذه الحزمة بالذات (بعد نقل الملفات إلى الهيكل الجديد وتطبيق تصحيحات المسارات)، وليس نسخًا من نتيجة تاريخية.**

| الطبقة | ملف الاختبار | العدد | النتيجة | يشغّل `Code.gs` الحقيقي؟ | عبر Sandbox أم شبكة حقيقية؟ |
|---|---|---|---|---|---|
| Node.js | `tests/test_harness.js` | 68 | 68/68 ✅ | نعم — نص `appsscript/Code.gs` الحرفي عبر `vm` | Sandbox (`tests/gs_sandbox.js`) — بلا شبكة |
| Rule Parity | `tests/test_rule_parity.js` | 14 سيناريو | 14/14 ✅ | نعم — يقارن `_validate()` الحقيقية بـ `clientValidateRules()` المُستخرَجة من `dashboard/index.html` | Sandbox — بلا شبكة |
| Playwright | `tests/test_dashboard.js` | 65 | 65/65 ✅ | نعم — نفس نص `Code.gs` عبر Sandbox، خلف `page.route()` | متصفح Chromium حقيقي، لكن الشبكة داخله معترَضة ومُوجَّهة محليًا لنفس Sandbox |
| **الإجمالي** | | **147** | **147/147 ✅** | | |

## ماذا يثبت هذا فعليًا

- منطق العمل في `Code.gs` نفسه سليم لكل القواعد المُختبرة (التحقق من الحقول الإلزامية، قواعد التواريخ، منع التكرار، حسابات المطابقة الصباحية، ...إلخ).
- واجهة `index.html` تتفاعل بشكل صحيح مع DOM حقيقي في متصفح حقيقي (نقر، تعبئة حقول، رسائل نجاح/خطأ، تمرير).
- تحقق الواجهة (`clientValidateRules`) متطابق مع تحقق الخادم (`_validate`) في كل السيناريوهات المُختبرة — لا فجوة معروفة بينهما حاليًا.

## ماذا **لا** يثبته هذا (بصراحة كاملة)

- **لا يثبت** أن نشر `Code.gs` كـ Web App حقيقي سينجح (أذونات، إعدادات "Execute as"/"Who has access"، حدود تنفيذ Google Apps Script اليومية، ...).
- **لا يثبت** أن الكتابة الفعلية داخل Google Sheets حقيقي ستنجح بنفس الشكل (تنسيق الخلايا، أداء عند آلاف الصفوف الحقيقية، تعارض كتابة متزامن من عدة مستخدمين حقيقيين).
- **لا يثبت** أن Idempotency (منع التكرار) يعمل بنفس الموثوقية عبر شبكة حقيقية (زمن استجابة، إعادة محاولة من طرف المتصفح، انتهاء صلاحية `CacheService` في Apps Script).
- **لا يغني** عن تنفيذ `docs/اختبار_Live_Integration.md` فعليًا قبل اعتماد النظام للإنتاج.

## إعادة تشغيل هذه الاختبارات بنفسك

```bash
npm install
npx playwright install chromium
npm run test:all
```

إن اختلفت النتيجة عمّا هو مذكور أعلاه بعد أي تعديل تُجريه على `appsscript/Code.gs` أو `dashboard/index.html`، فهذا الجدول لم يعد يعكس حالة الكود الحالية — أعد تشغيل الحزمة الثلاثية وحدّث الأرقام هنا وفي `README.md` وفقًا للنتيجة الفعلية الجديدة فقط.
