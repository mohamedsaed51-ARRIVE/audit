# اختبار Live Integration الحقيقي — لماذا لم يُنفَّذ من بيئة تجهيز هذه الحزمة، وكيف تنفّذه أنت

## الوضع الصريح

**لم يُختبر هذا المشروع بعد على Google Apps Script أو Google Sheets حقيقيين، من أي بيئة.** كل ما جرى اختباره هو منطق الكود محليًا (راجع `docs/تقرير_الاختبارات.md`)، عبر محاكاة في الذاكرة، بلا أي اتصال شبكي فعلي بخوادم Google.

## لماذا؟ (السبب موثَّق، وليس افتراضًا)

بيئة إعداد هذه الحزمة (بيئة سحابية معزولة تابعة لمساعد الذكاء الاصطناعي) محجوبة بالكامل عن الوصول لخدمات Google التالية على مستوى سياسة الشبكة:

```
$ curl -sS -m 10 https://script.google.com/
curl: (56) CONNECT tunnel failed, response 403

$ curl -sS -m 10 https://accounts.google.com/
curl: (56) CONNECT tunnel failed, response 403
```

بالإضافة إلى ذلك، الموصل الوحيد المتاح لخدمات Google في تلك البيئة (Google Drive) يعيد خطأ `Insufficient scope` حتى لعملية قراءة بسيطة، ولا يوجد أي موصل مخصص لـ Google Apps Script فيها إطلاقًا.

هذا الحجب على مستوى بيئة التطوير، **وليس على مستوى الكود أو المشروع نفسه** — لا يوجد أي سبب تقني في `Code.gs` أو `index.html` يمنع الاتصال الحقيقي؛ المشروع مصمَّم بالكامل للعمل مع Web App حقيقي (هذا هو الاستخدام الطبيعي والوحيد المقصود له).

## كيف تنفّذه أنت (من جهازك، بحساب Google حقيقي)

اتبع أولًا `docs/النشر.md` كاملًا لنشر `Code.gs` كـ Web App حقيقي على Google Sheet تجريبي منفصل عن الإنتاج. بعدها:

### 1. سجلات مقبولة (نوع واحد على الأقل لكل نوع من السبعة)

لكل نوع، من الواجهة الحقيقية المتصلة بالـ Web App الحقيقي:
1. أدخل بيانات صحيحة كاملة، وضع علامة اختبار واضحة (مثل `TEST-LIVE-<تاريخ اليوم>`) في حقل الملاحظات الصحيح حسب `schema.json`:
   - لكل الأنواع الستة الأخرى: حقل `notes`.
   - **لنوع "المشاكل اليومية" (`Issues`) تحديدًا: استخدم `auditor_notes` — هذا النوع الوحيد الذي لا يملك حقل `notes`** (تحقّق مباشرة من `backend/schema.json` قبل الإدخال).
2. سجّل: `clientRequestId` (يظهر في الكونسول أو يمكن التقاطه عبر أدوات المطوّر)، `recordId` الناتج في رسالة النجاح، عدد الصفوف في التاب قبل وبعد الحفظ (افتح الشيت في تبويب مجاور)، ووقت التنفيذ.
3. افتح الشيت وتأكد يدويًا أن الصف الجديد ظهر فعلًا، في التاب الصحيح، وأن `recordId` مطابق لما رجع من الواجهة.

### 2. سجلات مرفوضة

كرّر لكل حالة موضّحة في قواعد `Code.gs` (مثال: إجمالي مطابقة صباحية لا يساوي مجموع تصنيفاته، حالة "مفتوحة" مع تاريخ إغلاق، "مغلقة" بدون تاريخ إغلاق، جزاء "منفَّذ" بدون تاريخ تنفيذ):
- تأكد من ظهور رسالة خطأ واضحة في الواجهة.
- تأكد أن الشيت **لم** يكتسب صفًا جديدًا (عدد الصفوف قبل = عدد الصفوف بعد بالضبط).

### 3. Idempotency الحقيقي (نفس clientRequestId حرفيًا)

1. من أدوات المطوّر في المتصفح، التقط قيمة `currentClientRequestId` **قبل** الضغط على حفظ (تنبيه: تتغيّر هذه القيمة تلقائيًا بعد أي حفظ ناجح، فالتقاطها بعد الحفظ يُبطل الاختبار).
2. احفظ السجل الأول بشكل طبيعي، وسجّل `recordId` وعدد الصفوف.
3. من الكونسول، أعد إرسال نفس الطلب حرفيًا بنفس `clientRequestId` الملتقط (مثال: `apiPost({...، clientRequestId: 'القيمة الملتقطة'})`).
4. تحقق: هل رجع نفس `recordId`؟ هل بقي عدد الصفوف في الشيت كما هو دون إضافة صف جديد؟

**لا تُسجَّل هذه الخطوة "ناجحة" إلا إذا نُفِّذت فعليًا على الـ Web App الحقيقي مع تحقق مباشر من الشيت الحقيقي** — نتيجة هذا الاختبار داخل `tests/test_dashboard.js` (65/65) تثبت أن منطق `_dedupCheck` في الكود صحيح منطقيًا فقط، ولا تُغني عن هذه الخطوة.

### 4. تنظيف بيانات الاختبار

بعد الانتهاء، ابحث في الشيت عن كل صف يحمل علامة الاختبار التي استخدمتها، سجّل عددها، ثم احذفها يدويًا (حذف الصف الفعلي من الشيت). **لا تحذف أي بيانات إنتاجية حقيقية**، ولاحظ أن حذف صف لا يعيد تسلسل توليد `recordId` إلى الرقم السابق تلقائيًا (يبقى العداد كما هو في تاب `_Sequence`).

## متى يمكن اعتبار Live Integration "ناجحًا"

فقط بعد تنفيذ الخطوات 1–3 أعلاه فعليًا، والتحقق يدويًا من كل صف داخل الشيت الحقيقي بعينك — وليس بالاعتماد على نتيجة أي اختبار محلي (Node.js/Rule Parity/Playwright)، مهما كانت نسبة نجاحها.
